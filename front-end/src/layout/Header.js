import React from "react";
import Button from "../components/Button";

function Header() {
  return (
    <div>
      <Button
        className="font-bold bg-primary text-white py-2 px-5 rounded-full"
        text="Nisal"
      />
    </div>
  );
}

export default Header;
